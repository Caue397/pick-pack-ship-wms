# Copy this file to .env and fill in real values. .env is gitignored.

# HMAC-SHA256 signing secret shared by api-gateway and auth-service. Must be >= 32 bytes.
JWT_SECRET=

# Password for the auth-service Postgres database.
AUTH_DB_PASSWORD=

# Password for the order-service Postgres database.
ORDER_DB_PASSWORD=
